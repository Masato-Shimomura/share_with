<h2>プロフィール編集</h2>

  <%= form_with(model: resource, url: registration_path(resource_name), method: :put, local: true) do |f| %>
  <%= render "public/shared/error_messages", resource: resource %>

  <div class="field">
    <%= f.label :last_name, "姓" %><br />
    <%= f.text_field :last_name %>
  </div>

  <div class="field">
    <%= f.label :first_name, "名" %><br />
    <%= f.text_field :first_name %>
  </div>

  <div class="field">
    <%= f.label :email, "メールアドレス" %><br />
    <%= f.email_field :email, autocomplete: "email" %>
  </div>

  <div class="field">
    <%= f.label :current_password, "現在のパスワード（確認用）" %><br />
    <%= f.password_field :current_password, autocomplete: "current-password" %>
  </div>

  <div class="actions">
    <%= f.submit "更新する", class: "btn btn-primary" %>
  </div>
<% end %>

<%= link_to "マイページへ戻る", mypage_public_users_path, class: "btn btn-secondary", style: "margin-top: 20px;" %>

<div style="margin-top: 40px;">
  <p>退会をご希望の方は、以下のリンクから退会手続きを行ってください。</p>
  <%= link_to "退会はこちら", confirm_withdraw_public_users_path, class: "btn btn-outline-danger" %>
</div>
