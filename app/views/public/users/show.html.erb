<h2>ユーザー詳細</h2>

<p><strong>名前：</strong><%= @user.last_name %> <%= @user.first_name %></p>
<p><strong>メールアドレス：</strong><%= @user.email %></p>

<%= link_to "既存グループに招待", select_group_public_groups_path(user_id: @user.id), class: "btn btn-success" %>

<%= link_to "この人とグループを新規作成", invite_public_groups_path(invited_user_ids: [@user.id]), class: "btn btn-primary" %>

<%= link_to "ユーザー一覧に戻る", public_users_path, class: "btn btn-secondary", style: "margin-top: 20px;" %>